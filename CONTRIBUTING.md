# Contributing

Keep changes small and tested. Add or update an ADR for structural decisions. Domain code must not import `src/infrastructure` or `src/interfaces`. New external integrations require a domain port and contract tests before their adapter is added.
